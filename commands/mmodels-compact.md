---
description: Print a ready-to-paste /compact command with mmodels-aware focus instructions (preserves routing decisions, drops noise).
---

Print EXACTLY the line below and nothing else. The user will copy it and run it as their next message to trigger a focused compaction.

`/compact Preserve in this order: (1) the user's active task and any unresolved questions; (2) mmodels routing decisions — which tier handled which subtask and the one-line reason; (3) any ESCALATE events; (4) recently touched file paths and the nature of edits; (5) final outcomes of tier-agent runs. Drop verbose tool output, routine file reads, and intermediate exploration that did not change the plan.`
